services:
  api:
    image: ghcr.io/${GITHUB_REPOSITORY_OWNER}/poulpoulak:latest
    container_name: poulpoulak
    restart: unless-stopped
    env_file: .env
    volumes:
      - dong-data:/data
    # PTB uses long-polling by default; no ports to publish.

volumes:
  dong-data: