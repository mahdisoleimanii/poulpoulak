name: build & deploy

on:
  push:
    tags: ['v*']
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GHCR
        if: github.event_name == 'push'
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract tag name
        id: tag
        if: github.event_name == 'push'
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Build and push image
        if: github.event_name == 'push'
        uses: docker/build-push-action@v7
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/poulpoulak:latest
            ghcr.io/${{ github.repository_owner }}/poulpoulak:${{ steps.tag.outputs.version }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # 1. First, make sure the directory exists on the VPS
      - name: Prepare VPS Directory
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: mkdir -p "$HOME/poulpoulak"

      # 2. Copy your docker-compose.yml from the GitHub Runner to the VPS
      - name: Copy docker-compose.yml to VPS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          source: "docker-compose.yml"
          target: "poulpoulak/"

      # 3. Proceed with the deployment
      - name: Deploy to VPS over SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            set -euo pipefail
            cd "$HOME/poulpoulak"
            
            # Write runtime env vars from GitHub Secrets (no .env file needed in repo).
            cat > .env <<-ENVEOF
            BOT_TOKEN=${{ secrets.BOT_TOKEN }}
            SUPER_ADMINS=${{ secrets.SUPER_ADMINS }}
            PROXY_URL=${{ secrets.PROXY_URL }}
            DATA_DIR=/data
            ENVEOF
            
            # Pull the freshly built image.
            GITHUB_REPOSITORY_OWNER=${{ github.repository_owner }} \
              docker compose pull api || true
            GITHUB_REPOSITORY_OWNER=${{ github.repository_owner }} \
              docker compose up -d --remove-orphans
            docker image prune -f

      - name: Smoke-check the container
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            cd "$HOME/poulpoulak"
            docker compose ps
